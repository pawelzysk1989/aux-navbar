import { SideNavContentDirective } from './side-nav-content.directive';

describe('SideNavContentDirective', () => {
  it('should create an instance', () => {
    const directive = new SideNavContentDirective();
    expect(directive).toBeTruthy();
  });
});
